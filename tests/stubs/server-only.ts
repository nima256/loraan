/**
 * Stub for the `server-only` package.
 *
 * The real module throws when imported outside a React Server Component. The
 * services under test import it deliberately — it is what makes a client
 * component importing them a build error — so the test runner aliases it here.
 */
export {};
